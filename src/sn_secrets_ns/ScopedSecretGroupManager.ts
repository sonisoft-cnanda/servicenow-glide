import { GlideRecord } from "../types/GlideRecord";

export class ScopedSecretGroupManager {
    constructor() {  }
    isDuplicateCriteria(current?: GlideRecord): boolean {
        return false;
    }
}
