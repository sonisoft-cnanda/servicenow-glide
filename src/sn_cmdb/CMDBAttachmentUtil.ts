import { GlideRecord } from "../types/GlideRecord";

export class CMDBAttachmentUtil {
    constructor() {  }
    flushAttachment(tableRecord?: GlideRecord, fileName?: string, contentType?: string): string {
        return "";
    }
    streamContent(content?: string): void {
        
    }
}
