import { GlideRecord } from "../types/GlideRecord";

export class TransactionCallChainRegister {
    constructor() {  }
    updateRegisterRecord(register: GlideRecord): boolean {
        return false;
    }
}
