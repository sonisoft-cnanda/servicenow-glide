import { GlideScriptableInputStream } from "../types/GlideScriptableInputStream";

export class ExternalContentUtils {
    static closeStreamQuietly(streamToClose?: GlideScriptableInputStream): void {
        
    }
    static getPublicContentStream(url?: string): GlideScriptableInputStream {
        return null as any;
    }
}
