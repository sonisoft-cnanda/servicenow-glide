export class UpdateSetUtil {
    static getAppInstalls(rusSysId?: string, inHierarchy?: boolean, isLocal?: boolean): Array<any> {
        return [];
    }
    static hasAppInstalls(rusSysId?: string, inHierarchy?: boolean, isLocal?: boolean): boolean {
        return false;
    }
}
