import { GlideRecord } from "../types/GlideRecord";

export class CdsEvaluationService {
    constructor() {  }
    createEvaluationForModel(record?: GlideRecord): string {
        return "";
    }
}
