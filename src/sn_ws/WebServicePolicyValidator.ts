import { Context } from "../imports/Context";

export class WebServicePolicyValidator {
    constructor(cx: Context, args: any[], ctorObj: Function, inNewExpr: boolean) {  }
    validate(): boolean {
        return false;
    }
}
