import { GlideRecord } from "../types/GlideRecord";

export class HermesTopicsUtil {
    constructor() {  }
    isEnabled(): boolean {
        return false;
    }
    isTopicSynchronized(topicRecord?: GlideRecord): void {
        
    }
    syncHermesTopics(): void {
        
    }
}
