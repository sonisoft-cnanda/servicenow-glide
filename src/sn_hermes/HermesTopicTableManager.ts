export class HermesTopicTableManager {
    constructor() {  }
    createTopicRecord(topicName?: string, hermesServiceName?: string): string {
        return "";
    }
    syncPartitions(topicSysId?: string): boolean {
        return false;
    }
}
