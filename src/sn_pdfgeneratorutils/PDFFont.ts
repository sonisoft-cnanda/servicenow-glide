import { Paragraph } from "./Paragraph";

export class PDFFont {
    constructor(fontStyle: number, fontColor: any, fontSize: number, fontFamily: string) {  }
    process(label: string): Paragraph {
        return null as any;
    }
}
