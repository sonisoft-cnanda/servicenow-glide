import { GlideRecord } from "./GlideRecord";

export class TransactionReplayRunner {
    static doRun(setID: string, u_concurrent: number, u_replay_runs: number, u_skip_on_error: boolean, u_db_operations_on_tables: string, u_skip_transaction_type: string): GlideRecord {
        return null as any;
    }
}
