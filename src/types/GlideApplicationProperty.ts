export class GlideApplicationProperty {
    static getValue(name: string, domainSysId: string): string {
        return "";
    }
    static setValue(name: string, value: string, domainSysId: string): boolean {
        return false;
    }
}
