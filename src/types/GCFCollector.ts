import { GCFSampleMap } from "./GCFSampleMap";

export class GCFCollector {
    static recordUsageEvent(category: string, instrumentationPoint: string, instrumentationEvent: string, sampleMap: GCFSampleMap): void {
        
    }
}
