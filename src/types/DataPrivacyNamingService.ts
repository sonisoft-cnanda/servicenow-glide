export class DataPrivacyNamingService {
    constructor() {  }
    generateResourceName(requestJson: string): string {
        return "";
    }
    parseResourceName(resourceName: string): string {
        return "";
    }
}
