export class GlideAppCache {
    static clear(): void {
        
    }
    static get(key: string): any {
        return null as any;
    }
    static put(key: string, value: any): void {
        
    }
    static remove(key: string): void {
        
    }
}
