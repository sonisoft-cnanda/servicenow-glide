import { Outcome } from "./Outcome";
import { GlideRecord } from "../types/GlideRecord";

export class GlideHelper {
    static update(record?: GlideRecord, po?: Outcome, updateField?: string): void {
        
    }
}
