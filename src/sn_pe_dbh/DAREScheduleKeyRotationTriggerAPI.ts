import { GlideRecord } from "../types/GlideRecord";

export class DAREScheduleKeyRotationTriggerAPI {
    constructor() {  }
    triggerKeyRotation(grTrigger: GlideRecord): void {
        
    }
    triggerMonitor(): void {
        
    }
    triggerNotification(grTrigger: GlideRecord): void {
        
    }
}
