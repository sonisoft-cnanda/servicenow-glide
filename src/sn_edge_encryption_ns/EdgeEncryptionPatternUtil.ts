export class EdgeEncryptionPatternUtil {
    constructor() {  }
    doesPatternCompile(pattern: string[]): boolean {
        return false;
    }
    doesPatternMatchSample(pattern: string, sample: string): boolean {
        return false;
    }
    isPatternAllowed(pattern: string[]): boolean {
        return false;
    }
    isPatternCharAllowed(pattern: string): boolean {
        return false;
    }
    isSampleAllAlpha(sample: string): boolean {
        return false;
    }
}
