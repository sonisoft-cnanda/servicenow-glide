import { GlideRecord } from "../types/GlideRecord";

export class ReleaseOperationManager {
    constructor() {  }
    handleRemoteOperationRequest(requestMessage?: string): GlideRecord {
        return null as any;
    }
    handleRemoteOperationUpdate(updateMessage?: string): GlideRecord {
        return null as any;
    }
    startOperation(targetInstanceName?: string, operationRequestStr?: string): GlideRecord {
        return null as any;
    }
    watchOperationTrackers(): void {
        
    }
}
