import { GlideRecord } from "../types/GlideRecord";

export class ContinuousAuthenticationUtil {
    constructor() {  }
    getContinuousAuthenticationPolicyForDataClass(classificationSysId: Array<string>): Map<string, GlideRecord> {
        return {} as any;
    }
    isContinuousAuthenticationAppliedToDataClass(classificationSysId: Array<string>): Map<string, boolean> {
        return {} as any;
    }
    static manageACLForPolicy(policyGR: GlideRecord): void {
        
    }
}
