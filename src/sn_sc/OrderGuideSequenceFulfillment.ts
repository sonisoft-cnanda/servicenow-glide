import { GlideRecord } from "../types/GlideRecord";

export class OrderGuideSequenceFulfillment {
    static fulfillSequencedOGItem(request?: GlideRecord, item?: GlideRecord): GlideRecord {
        return null as any;
    }
}
