export class ISVDefinition {
    static calculateMetricHash(sysAppId: string): string {
        return "";
    }
    static checkDefinition(sysAppId: string, publishToStore: boolean): void {
        
    }
}
