import { GlideRecord } from "../types/GlideRecord";

export class WorkItemDataGenerator {
    static clean(): void {
        
    }
    static generateFromProducer(producer?: GlideRecord): void {
        
    }
}
