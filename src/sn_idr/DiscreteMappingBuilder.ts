import { GlideRecord } from "../types/GlideRecord";

export class DiscreteMappingBuilder {
    constructor() {  }
    build(): GlideRecord {
        return null as any;
    }
    discreteValue(discreteValue?: string): DiscreteMappingBuilder {
        return null as any;
    }
    getErrors(): Array<any> {
        return [];
    }
    hasErrors(): boolean {
        return false;
    }
    outboundEntry(outboundEntry?: GlideRecord): DiscreteMappingBuilder {
        return null as any;
    }
    referencedTable(referencedTable?: string): DiscreteMappingBuilder {
        return null as any;
    }
    subscription(subscription?: GlideRecord): DiscreteMappingBuilder {
        return null as any;
    }
    static withSysId(sysId?: string): DiscreteMappingBuilder {
        return null as any;
    }
    static withValues(discreteValue?: string, referencedTable?: string, outboundEntry?: GlideRecord, subscription?: GlideRecord): DiscreteMappingBuilder {
        return null as any;
    }
}
