import { PayloadScannerResult } from "./PayloadScannerResult";

export class PayloadScanner {
    constructor() {  }
    scan(sourceId?: string, sourceTableName?: string, reuseTemplateApp?: boolean): PayloadScannerResult {
        return null as any;
    }
}
