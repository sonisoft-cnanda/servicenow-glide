import { AddressFilterResult } from "./AddressFilterResult";

export class AddressFilter {
    static filter(emailAddressFilterIds?: string, address?: string): AddressFilterResult {
        return null as any;
    }
}
