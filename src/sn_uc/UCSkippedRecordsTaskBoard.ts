import { GlideRecord } from "../types/GlideRecord";

export class UCSkippedRecordsTaskBoard {
    static syncUpgradeHistoryLogRecord(current?: GlideRecord, previous?: GlideRecord): void {
        
    }
    static syncUpgradeHistoryTaskRecord(current?: GlideRecord, previous?: GlideRecord): void {
        
    }
}
