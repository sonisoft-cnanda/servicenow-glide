export class GlideSystemRunLevel {
    static getCurrentLevel(): string {
        return "";
    }
    static getToggleExceptionRoles(toggle: string): string {
        return "";
    }
    static getToggleValue(toggle: string): number {
        return 0;
    }
    static switchLevel(newLevel: string): string {
        return "";
    }
}
